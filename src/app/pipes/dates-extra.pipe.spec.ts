import { DatesExtraPipe } from './dates-extra.pipe';

describe('DatesExtraPipe', () => {
  it('create an instance', () => {
    const pipe = new DatesExtraPipe();
    expect(pipe).toBeTruthy();
  });
});
